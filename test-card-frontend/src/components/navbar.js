import React from 'react';
import PropTypes from 'prop-types';

navbar.propTypes = {
    
};

function NavBar(props) {
    return (
        <div>
            
        </div>
    );
}

export default NavBar;